---
phase: 01-gmail-ingest
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ingest/gmail_sync.py
  - ingest/watch.py
  - scripts/gcp_setup.ps1
  - scripts/gmail_oauth.py
  - Dockerfile
  - .env.example
  - README.md
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
user_setup:
  - Google Cloud project id (gcloud CLI install if missing)
  - Dedicated Gmail + OAuth client (Gmail API + Sheets later)
must_haves:
  truths:
    - users.watch request body includes topicName and INBOX label filter
    - README documents 7-day watch expiry and daily renewal
    - Poll fallback lists new messages when PUBSUB_FALLBACK_POLL is true
  artifacts:
    - ingest/gmail_sync.py
    - ingest/watch.py
    - scripts/gcp_setup.ps1
    - Dockerfile
  key_links:
    - /internal/watch-renew calls users.watch
    - /internal/poll uses history or list then handler
---

<objective>
Gmail watch + Pub/Sub (or Scheduler poll fallback) + watch renewal docs/scripts for the existing GCP account.
</objective>

<context>
@.planning/phases/01-gmail-ingest/01-01-PLAN.md
@docs/HARNESS.md
</context>

<tasks>

<task type="auto">
  <name>Gmail sync, watch renew, GCP scripts</name>
  <files>ingest/gmail_sync.py, ingest/watch.py, tests/test_gmail_sync.py, tests/test_watch.py, scripts/gcp_setup.ps1, scripts/gmail_oauth.py, Dockerfile, .env.example, README.md</files>
  <action>
History sync maps Gmail messages to attachments + Message-Id. Watch builder sets topicName and labelIds INBOX, documents 7-day expiry. PowerShell setup enables APIs, creates topic/subscription placeholders, Cloud Run deploy, Scheduler jobs. OAuth helper stores refresh token for dedicated inbox.
  </action>
  <verify>python -m pytest tests/ -q</verify>
  <done>Watch JSON contains topicName; README mentions 7 days; tests pass</done>
</task>

</tasks>
